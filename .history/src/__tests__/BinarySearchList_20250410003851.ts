import binary_fn from "@code/BinarySearchList"

export default function bs_list() {haystack:Number[], neddle:Number): Boolean{
    let low = 0 ; 
    let high = haystack.length;
    do{
        const mid = Math.floor(low + (high - low)/2);
    }while()
}

